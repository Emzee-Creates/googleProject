export default function Footer() {
  return (
    <footer className="text-center py-2 text-sm text-gray-500 border-t">
      © {new Date().getFullYear()} AI Risk + Yield Optimizer
    </footer>
  );
}
